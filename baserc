alias python=python3
alias pip="python3 -m pip"

if [ $TMUX ]; then
    source "$HOME/$(basename $SHELL)rc"
    clear
fi

function gpa()
{
    git pull
    git fetch --all
    git branch -r
}

function name()
{
    uname -a | cut -d' ' -f2
}
